#!/usr/bin/env bash

args=("$@")
dir=${args[-1]}
[[ -v $JOT_HOOK ]] || echo "Please set JOT_HOOK to the absolute path of the jot post-commit hook." ; exit 1

echo "cargo $args"


echo "ln -s $hook $dir/.git/hooks"
